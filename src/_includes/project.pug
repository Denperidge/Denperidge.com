---
layout: base/base.pug
tags: Project
permalink: "projects/{{title}}/"
---

main
    h1=title
    
    h2.sr-only Intro
    p=intro
    
    h2 Achievements
    section.markdown-render=achievements
    
    h2 Future improvements
    section.markdown-render=future_improvements

    if hrefs
        h2 Links:
        ul
            each link in hrefs
                -
                    let url = link.url.toLowerCase();
                    let host = null;
                    let text;
                    if (url.includes("github")) { host = "GitHub" }

                    if (host) { text = `View the ${link.name} repo on ${host}`; }
                    else { text = `Visit the ${title} website`; }
                li 
                    a(href=link.url)=text
    
